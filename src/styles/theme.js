// Colors
const colors = {
  black: '#333333',
  gray: '#828282',
  white: '#fff',

  yellow: '#F2C94C',
}

// Fonts
const fonts = {
  main: `'Archivo', sans-serif`,
}

// Shadows
const shadows = {
  cardShadow: `0px 3px 6px rgba(34, 34, 34, 0.1)`
}

export default {
  ...colors,
  ...fonts,
  ...shadows
}